<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>S&F- your AI</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet">
</head>
<body>
 <h1 class="SFmaintext">Stone & Flinch</h1>
 <div class="image-container">
 <img src="an AI for business.png" id="imageslayyy" alt="AI on phone and computer"></img>
 <div class="inside-text">Your AI for Business</div>
  <div class="descripition">Make your bussines launch with AI by your side.</div>
  <button class="buttone">Get started &#8599; </button>
  <textarea id="ttt">helo</textarea>
</div>
    
</body>
<style>
/* 🔹 Fade-in animation (only opacity) */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* Apply fade-in to text, description, and button */
.inside-text,
.descripition,
.buttone {
  opacity: 0; /* hidden at first */
  animation: fadeIn 1s ease forwards; /* fade in */
}

#ttt{
    position: absolute;
    color: white;
    background-color: green;
}








    .buttone{
         animation-delay: 0.9s;
        position: absolute;
      top: 16%;   /* vertical center */
      left: 50%;  /* horizontal center */
      transform: translate(-50%, -50%); /* perfect centering */
      background-color: white;
      color: black;
      font-family: "Manrope", sans-serif;
      border: none;
      padding: 12px 30px;
      border-radius: 25px;
      font-weight: 600;
      transition: all 0.3s ease;

      
    }

    .buttone:hover{
     
  transform: translate(-50%, -50%) scale(1.05); /* slight grow */
  box-shadow: 0 0 15px rgba(255, 11, 235, 0.4); /* glow effect */
    }

      .image-container {
      position: relative;   /* allows text positioning */
      display: inline-block;
    }

    .descripition{
        animation-delay: 0.6s;
         position: absolute;
      top: 13%;   /* vertical center */
      left: 50%;  /* horizontal center */
      transform: translate(-50%, -50%); /* perfect centering */
      color: white;
      font-family: "Manrope", sans-serif;
      font-size: 1rem;
      font-weight: 600;
    }

     .inside-text {
        animation-delay: 0.3s;
      position: absolute;
      top: 10%;   /* vertical center */
      left: 50%;  /* horizontal center */
      transform: translate(-50%, -50%); /* perfect centering */
      color: white;
      font-family: "Manrope", sans-serif;
      font-size: 3rem;
      font-weight: 600;
     
    }

     #imageslayyy{
         display: block;
  margin: 0 auto;   /* centers image */
  border-radius: 25px;
  max-width: 80%;   /* makes it responsive */
  height: auto;
  border-top: 2px solid white;    /* white border on top */
  border-bottom: 2px solid white; 
     }
    .SFmaintext{
        font-family: "Manrope", sans-serif;
  font-optical-sizing: auto;
  font-weight: 600;
  font-style: normal;
  color: white;
    }

    body{
        background-color: black;
         text-align: center; /* centers text AND image */
    }
</style>

</html>
